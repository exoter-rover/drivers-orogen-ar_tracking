# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(ar_trackingTaskLib)
ADD_LIBRARY(${AR_TRACKING_TASKLIB_NAME} SHARED 
    ${AR_TRACKING_TASKLIB_SOURCES})
add_dependencies(${AR_TRACKING_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${AR_TRACKING_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${AR_TRACKING_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${AR_TRACKING_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${AR_TRACKING_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${AR_TRACKING_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${AR_TRACKING_TASKLIB_HEADERS}
    DESTINATION include/orocos/ar_tracking)

